# Loopback eBPF

This document describes how to build and run the loopback-ebpf example.

### Prerequisites

- May need to install extra cargo toolchains

### Running the example

Uunder current directory, simply run: 
```sh
cargo build --release; docker compose up --build
```

