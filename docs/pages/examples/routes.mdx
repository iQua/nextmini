## Running the Routes Example

As described in the simple example page, there are four type of preset topology supported in Nextmini: `full_mesh`, `ring`, `fat_tree`, and `torus`.

### Running ring example

After customizing edges or using built-in the preset topology to define network topology,