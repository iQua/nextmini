# PyTorch + Nextmini Python API quickstart

The canonical quickstart now lives at `docs/docs/examples/pytorch_python_api.md` so it can be published via MkDocs.
Please edit that file for any future updates. This placeholder remains so existing references (e.g., in `AGENTS.md` and
legacy READMEs) continue to point developers to the right document.
