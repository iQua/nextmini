[project]
name = "strato-dashboard"
version = "0.1.0"
description = "A simple dashboard for monitoring Strato throughput at runtime."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "psycopg2-binary",
    "rich",
    "numpy"
]
