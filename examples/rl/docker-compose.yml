services:
  postgres:
    image: postgres:16-alpine
    container_name: rl-postgres
    hostname: postgres
    init: true
    restart: unless-stopped
    environment:
      POSTGRES_USER: pgusr
      POSTGRES_PASSWORD: pgpwrd
      POSTGRES_DB: nextmini
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "pgusr", "-d", "nextmini"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 10s
    volumes:
      - ../../controller/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      rl_net:
        ipv4_address: 172.31.2.2

  controller:
    build:
      context: ../../
      dockerfile: controller/Dockerfile
    image: nextmini_controller
    container_name: rl-controller
    hostname: controller
    init: true
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      RUST_LOG: "${RUST_LOG:-info}"
    volumes:
      - ../../:/workspace:cached
      - ./configs-docker/controller-config.toml:/var/nextmini/config.toml
    working_dir: /var/nextmini
    command: /bin/bash -lc "sleep 5; exec /var/nextmini/controller"
    ports:
      - "3000:3000"
    networks:
      rl_net:
        ipv4_address: 172.31.2.3

  trainer:
    build:
      context: ../../
      dockerfile: examples/rl/Dockerfile
    image: nextmini_rl_python
    container_name: rl-trainer
    hostname: trainer
    init: true
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    gpus: all
    depends_on:
      controller:
        condition: service_started
    volumes:
      - ../../:/workspace:cached
      - ./output:/workspace/examples/rl/output
    working_dir: /workspace
    environment:
      RUST_LOG: "${RUST_LOG:-info}"
      SKIP_BUILD: "${SKIP_BUILD:-0}"
      NEXTMINI_PY_FORCE_REBUILD: "${NEXTMINI_PY_FORCE_REBUILD:-1}"
      TORCH_INDEX_URL: "${TORCH_INDEX_URL:-https://download.pytorch.org/whl/cu124}"
      NEXTMINI_PY_WHEEL: "${NEXTMINI_PY_WHEEL:-}"
      CUDA_VISIBLE_DEVICES: "0"
    command:
      - bash
      - -lc
      - >
        exec examples/rl/scripts/run_rl_node.sh trainer
        examples/rl/configs-docker/trainer-config.toml
    networks:
      rl_net:
        ipv4_address: 172.31.2.10

  worker_0:
    build:
      context: ../../
      dockerfile: examples/rl/Dockerfile
    image: nextmini_rl_python
    container_name: rl-worker-0
    hostname: worker-0
    init: true
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    gpus: all
    depends_on:
      controller:
        condition: service_started
    volumes:
      - ../../:/workspace:cached
    working_dir: /workspace
    environment:
      RUST_LOG: "${RUST_LOG:-info}"
      SKIP_BUILD: "${SKIP_BUILD:-0}"
      NEXTMINI_PY_FORCE_REBUILD: "${NEXTMINI_PY_FORCE_REBUILD:-1}"
      TORCH_INDEX_URL: "${TORCH_INDEX_URL:-https://download.pytorch.org/whl/cu124}"
      NEXTMINI_PY_WHEEL: "${NEXTMINI_PY_WHEEL:-}"
      CUDA_VISIBLE_DEVICES: "1"
    command:
      - bash
      - -lc
      - >
        exec examples/rl/scripts/run_rl_node.sh worker
        examples/rl/configs-docker/worker0-config.toml
        --rank 0
        --gpu 0
    networks:
      rl_net:
        ipv4_address: 172.31.2.11

  worker_1:
    build:
      context: ../../
      dockerfile: examples/rl/Dockerfile
    image: nextmini_rl_python
    container_name: rl-worker-1
    hostname: worker-1
    init: true
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    gpus: all
    depends_on:
      controller:
        condition: service_started
    volumes:
      - ../../:/workspace:cached
    working_dir: /workspace
    environment:
      RUST_LOG: "${RUST_LOG:-info}"
      SKIP_BUILD: "${SKIP_BUILD:-0}"
      NEXTMINI_PY_FORCE_REBUILD: "${NEXTMINI_PY_FORCE_REBUILD:-1}"
      TORCH_INDEX_URL: "${TORCH_INDEX_URL:-https://download.pytorch.org/whl/cu124}"
      NEXTMINI_PY_WHEEL: "${NEXTMINI_PY_WHEEL:-}"
      CUDA_VISIBLE_DEVICES: "2"
    command:
      - bash
      - -lc
      - >
        exec examples/rl/scripts/run_rl_node.sh worker
        examples/rl/configs-docker/worker1-config.toml
        --rank 1
        --gpu 0
    networks:
      rl_net:
        ipv4_address: 172.31.2.12

networks:
  rl_net:
    driver: bridge
    ipam:
      config:
        - subnet: 172.31.2.0/24
