# Environment variables for docker-compose
RUST_LOG=info
SKIP_BUILD=0
# Set SKIP_BUILD=1 to use pre-built wheel (faster startup)
# First build the wheel: cd ../../python-api && maturin build --release -F python-extension
