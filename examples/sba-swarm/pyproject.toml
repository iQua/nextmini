[project]
name = "pytorch-mpi"
version = "0.1.0"
description = "A simple dashboard for monitoring nextmini throughput at runtime."
readme = "README.md"
requires-python = "==3.13.*"
dependencies = [
    "torch",
    "torchvision",
    "transformers",
    "datasets",
    "tqdm"
]
