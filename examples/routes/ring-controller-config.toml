# To test ring topology, run: `cp ring-controller-config.toml controller-config.toml`
protocol = "tcp"

[topology]
type = "ring"
ring_config = { n_nodes = 4 }

# Set the routing protocol to "shortest_path" if routes in the topology are needed.
[routing]
protocol = "shortest_path"
