
node_id = 2


public_network_interface = "eth0"


# Use a real public IP when deploying. The default value is a documentation placeholder (RFC 5737).
public_network_addr = "203.0.113.11"

private_network_name = "vm2-private"
# private_network_interface = "eth1"
# private_network_port = "8081"

num_tun_queues = 1
num_packet_processors = 1
channel_capacity = 4000
queue_capacity = 3000
feature = "concurrent"
