volumes:
  postgres-data:

services:
  postgres:
    image: postgres:alpine
    hostname: postgres
    environment:
      POSTGRES_USER: pgusr
      POSTGRES_PASSWORD: pgpwrd
      POSTGRES_DB: nextmini
    ports:
      - "127.0.0.1:5432:5432" # Only bind to localhost for security
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "pgusr", "-d", "nextmini"]
      interval: 5s
      timeout: 3s
      retries: 30
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ../../controller/init.sql:/docker-entrypoint-initdb.d/init.sql
    restart: always

  controller:
    image: nextmini_controller
    hostname: controller
    build:
      context: ../../
      dockerfile: ./controller/Dockerfile
    ports:
      - "0.0.0.0:3000:3000" # Bind to all interfaces for external access from Fly.io nodes
    volumes:
      - ./controller-config.toml:/var/nextmini/config.toml
    depends_on:
      postgres:
        condition: service_healthy
    restart: always
    command: /bin/bash -c "sleep 5 && /var/nextmini/controller"
