[project]
name = "nextmini-localserver-flyio-deploy"
version = "0.1.0"
description = "Hybrid deployment: Controller on local server, Dataplane on Fly.io"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.32.3",
]
