[project]
name = "lp-multicast"
version = "0.1.0"
description = "LP helpers for multicast trees in Nextmini"
readme = "README.md"
requires-python = "==3.13.*"
dependencies = [
    "numpy",
    "cvxopt",
]
