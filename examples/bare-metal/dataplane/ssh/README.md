Local SSH credentials for the bare-metal example live in this directory but are not tracked.

Generate a key pair locally before running the example:

```bash
ssh-keygen -t rsa -b 4096 -f id_rsa -N ""
```
