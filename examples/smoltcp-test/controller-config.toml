protocol = "tcp"

[topology]
type = "full_mesh"
n_nodes = 2

[[flows]]
src_node_id = 1
dst_node_id = 2
flow_size = { Bytes = 1_000_000_000_0 }

# add another flow for testing purposes
# Q: if we are using only one tcp connection between nodes,
# do we compete for the same connection?
# then the two flows will be competing for the same connection
[[flows]]
src_node_id = 2
dst_node_id = 1
flow_size = { Bytes = 1_000_000_000_0 }
