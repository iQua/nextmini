# External client and server guide

You should first follow the `./multi-dc/README.md` to depoly basic `Nextmini` across multiple instances. On manager instance, you could deploy server and client to what ever nodes. Then, run the `route/insert_route.py` with uv to insert routes into the database.
